#include <iostream>

void promote() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    std::cout.tie(0);
    return;
}

const int maxN = 500;
const int maxM = 100;
const int maxT = 4e6;

int n, m;
int t[maxN + 10];

int main() {
    promote();
    std::cout << 0;
}